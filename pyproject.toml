[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "endstone-tg_bot"
version = "1.1"

[project.entry-points."endstone"]
tg_bot = "endstone_tg_bot:Main"